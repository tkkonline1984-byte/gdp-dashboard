# คู่มือ TKK ONLINE Product Intake Hub v2.0.0

## 1. สำหรับพนักงาน

1. เปิดลิงก์ระบบจากมือถือหรือคอมพิวเตอร์
2. ใส่รหัสเข้าใช้งานที่บริษัทแจ้ง
3. เลือกเมนู **ส่งรูปสินค้า**
4. เลือก **ถ่ายรูปตอนนี้** หรือ **เลือกรูปจากเครื่อง**
5. กรอกชื่อพนักงาน แผนก/สาขา และรหัสใต้บาร์โค้ด 13 หลัก
6. ตรวจรูปและเลขให้ถูกต้อง ติ๊กยืนยัน แล้วกด **ส่งข้อมูลเข้าบริษัท**
7. ต้องเห็นข้อความ **ส่งสำเร็จ** และเลขรับรายการก่อนปิดหน้า

### รูปที่เหมาะกับการส่ง

- ถ่ายในที่สว่างและไม่ย้อนแสง
- เห็นสินค้าครบ ไม่ตัดขอบ
- กล้องไม่สั่น ตัวหนังสือบนบรรจุภัณฑ์อ่านได้
- หลีกเลี่ยงข้อมูลส่วนตัวหรือใบหน้าบุคคลที่ไม่เกี่ยวข้อง

## 2. สำหรับผู้ดูแล

1. เปิดเมนู **รายการที่ส่งแล้ว**
2. ใส่ `ADMIN_CODE`
3. ค้นหาด้วยรหัสสินค้า ชื่อสินค้า ชื่อพนักงาน หรือสาขา
4. ดาวน์โหลด CSV เพื่อส่งต่อให้งานในองค์กร
5. เลือกรายการเพื่อดูและดาวน์โหลดรูปต้นฉบับ

ข้อมูลจัดเก็บเป็น JPG + JSON โดยวันที่และรหัสสินค้า จึงเชื่อมต่อระบบอัตโนมัติต่อได้ง่าย

## 3. วิธีใช้เครื่องมือแปลงไฟล์

### PDF → JPG

เลือก PDF → เลือก DPI (แนะนำ 200) → กดแปลง → ดาวน์โหลด ZIP รูปทุกหน้า

### PDF → Excel

เลือก PDF → กดแปลง → ดาวน์โหลด XLSX ระบบพยายามอ่านตารางก่อน ถ้าเป็นเอกสารสแกนจึงใช้ OCR

### JPG/PNG → Excel

เลือกรูปได้หลายไฟล์ → กดอ่านรูป → ดาวน์โหลด XLSX แต่ละรูปจะแยก Worksheet พร้อมข้อความ ตำแหน่ง และคะแนนความมั่นใจ

### Excel → PDF

เลือก XLSX/XLS → กดแปลง → ดาวน์โหลด PDF ระบบรวมทุก Worksheet และทำหัวตารางซ้ำทุกหน้า

> ทุกงาน OCR ต้องมีพนักงานตรวจทานก่อนนำเข้า POS/บัญชี เพราะรูปเบลอ แสงสะท้อน หรือตารางซับซ้อนอาจทำให้บางตัวอักษรผิดได้

## 4. ตั้งค่าที่เก็บข้อมูลอย่างปลอดภัย

Repository นี้เป็นพื้นที่เผยแพร่โค้ดสาธารณะ จึงไม่ระบุชื่อที่เก็บข้อมูลภายใน รหัส หรือรายละเอียดสิทธิ์การเข้าถึงไว้ในคู่มือนี้

ให้ผู้ดูแลระบบที่ได้รับอนุญาตกำหนดที่เก็บข้อมูลแบบ Private และค่าลับผ่านระบบ Secrets ของแพลตฟอร์มเท่านั้น ห้ามใช้ Repository โค้ดสาธารณะเก็บรูปสินค้า ข้อมูลพนักงาน หรือข้อมูลภายในองค์กร

ห้าม Commit `.streamlit/secrets.toml` และห้ามส่งข้อมูลลับทาง LINE/อีเมล/แชต

## 5. ตั้งค่า Streamlit Secrets

ใช้แม่แบบ `.streamlit/secrets.toml.example`:

```toml
APP_ACCESS_CODE = "รหัสพนักงานที่ตั้งใหม่"
ADMIN_CODE = "รหัสผู้ดูแลอีกชุดหนึ่ง"
STORAGE_MODE = "github"
GITHUB_REPOSITORY = "เจ้าของ/ชื่อ-private-repository"
GITHUB_BRANCH = "main"
GITHUB_DATA_ROOT = "submissions"
GITHUB_TOKEN = "กำหนดในระบบ Secrets ส่วนตัวเท่านั้น"
ALLOW_PUBLIC_STORAGE = false
STRICT_EAN13 = true
```

`STRICT_EAN13 = true` ตรวจ Check Digit จริง ถ้ารหัสบริษัทเป็นเลข 13 หลักภายในที่ไม่ใช่ EAN-13 ให้ตั้งเป็น `false`

## 6. ติดตั้งบน Streamlit Community Cloud

1. ไปที่ `https://share.streamlit.io/`
2. Sign in with GitHub และเลือก New app
3. Repository: `tkkonline1984-byte/gdp-dashboard`
4. Branch: `main`
5. Main file path: `streamlit_app.py`
6. Advanced settings → Secrets → วางค่าจากข้อ 5
7. กด Deploy
8. เปิด URL ด้วยมือถือจริงและทดสอบตาม `DEPLOY_CHECKLIST.md`

## 7. ใช้บน Windows

1. ติดตั้ง Python 3.12/3.13 แบบ 64-bit และเลือก Add Python to PATH
2. คัดลอก `.streamlit\secrets.toml.example` เป็น `.streamlit\secrets.toml`
3. แก้ Secrets ด้วย Notepad
4. ดับเบิลคลิก `install.bat` ครั้งแรก
5. เปิดระบบด้วย `run.bat`
6. ถ้าต้องการทดสอบโดยไม่ส่ง GitHub ให้ตั้ง `STORAGE_MODE = "local"`

Local mode เก็บที่ `local_submissions` และไม่เหมาะเป็นที่เก็บถาวรบน Cloud

## 8. แก้ปัญหาที่พบบ่อย

| อาการ | วิธีแก้ |
|---|---|
| ระบบแจ้ง Repository เป็น Public | เปลี่ยน Repository เป็น Private หรือใช้ Private data repository |
| ระบบจัดเก็บยังไม่พร้อม | ให้ผู้ดูแลตรวจค่าที่เก็บข้อมูลและ Secrets ในพื้นที่ส่วนตัว |
| พนักงานส่งไม่ได้ | ตรวจอินเทอร์เน็ตและติดต่อผู้ดูแลระบบ |
| รหัส 13 หลักไม่ผ่าน | ตรวจเลขหลักสุดท้าย หรือปิด Strict EAN-13 เฉพาะเมื่อเป็นรหัสภายใน |
| OCR ไทยไม่ทำงาน | บน Cloud ตรวจ `packages.txt`; Windows ติดตั้ง Tesseract พร้อมภาษา Thai |
| Excel ออกมาไม่ตรงตาราง | ใช้ PDF ที่มี Text/Table จริง หรือปรับภาพให้ตรงและชัด แล้วตรวจทานผล OCR |
| รูปกล้องไม่เปิด | อนุญาตสิทธิ์ Camera ใน Browser และใช้ HTTPS บนระบบออนไลน์ |

## 9. การดูแลระบบ

- หมุนเวียน `APP_ACCESS_CODE` และ `ADMIN_CODE` ตามรอบบริษัท
- ยกเลิกข้อมูลลับเก่าทันทีเมื่อสงสัยว่ารั่ว
- ทบทวนและหมุนเวียนข้อมูลลับตามนโยบายบริษัท
- สำรอง Repository เก็บข้อมูลตามนโยบายบริษัท
- ตรวจ GitHub Actions หลังอัปเดตทุกครั้ง
- จำกัดผู้มีสิทธิ์ Repository เท่าที่จำเป็น
